<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<script>
			 //数组操作
			 var arr1=[10,20,30];
			 
			 function getMax(arr)
			 {
				 var max=arr[0];
				 for (var i=0;i<arr.length;i++) {
				 	if(max<arr[i])
					{
						max=arr[i];
					}
				 }
				 return max;
			 }
			 var $max=getMax(arr1);
			 //alert("$max="+$max);
			 document.write("$max="+$max);
		</script>
		<div onclick="alert('hello world!')">hello</div>
	</body>
</html>

结果：在页面中显示：$max=30
